// Exemplo de configuração PM2
// Copie para ecosystem.config.js e ajuste conforme necessário

module.exports = {
  apps: [{
    name: 'souls-hunter-bot',
    script: './index.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    env: {
      NODE_ENV: 'production'
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_file: './logs/combined.log',
    time: true
  }]
};
